@model List<CAPS.Controllers.StaffWithAppointmentCount>

@{
    ViewData["Title"] = "Staff";
}

<link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet" />

<div class="container my-3">
    <div class="d-flex justify-content-between align-items-center mb-2">
        <h4 class="mb-0">@ViewData["Title"]</h4>
        <a asp-action="UpSert" class="btn btn-sm btn-primary">
            <i class="bi bi-plus-lg"></i> Add
        </a>
    </div>

    <div class="table-responsive">
        <table class="table table-sm table-bordered table-striped align-middle mb-0">
            <thead class="table-dark small">
                <tr>
                    <th>Name</th>
                    <th>Gender</th>
                    <th>Expertise</th>
                    <th>Email</th>
                    <th>Phone</th>
                    <th>Hired</th>
                    <th>Appointments</th>
                    <th>Active</th>
                    <th class="text-center">Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr class="small">
                        <td>@item.Staff.FullName</td>
                        <td>@item.Staff.Gender</td>
                        <td>@item.Staff.Expertise</td>
                        <td class="text-truncate" style="max-width: 150px;">@item.Staff.Email</td>
                        <td>@item.Staff.PhoneNumber</td>
                        <td>@item.Staff.DateHired.ToString("yyyy-MM-dd")</td>
                        <td>
                            <div class="d-flex flex-column align-items-start">
                                <span class="badge bg-primary mb-1" title="Total appointments">
                                    <i class="bi bi-calendar-event"></i> @item.TotalAppointments
                                </span>
                                <span class="badge bg-success" title="Upcoming appointments">
                                    <i class="bi bi-clock"></i> @item.UpcomingAppointments
                                </span>
                            </div>
                        </td>
                        <td>
                            <span class="badge @((item.Staff.IsActive ? "bg-success" : "bg-secondary"))">
                                @(item.Staff.IsActive ? "Yes" : "No")
                            </span>
                        </td>
                        <td class="text-center">
                            <a asp-action="UpSert" asp-route-id="@item.Staff.StaffId" class="btn btn-sm btn-outline-warning" title="Edit">
                                <i class="bi bi-pencil"></i>
                            </a>
                            <a asp-controller="Appointment" asp-action="Index" asp-route-staffId="@item.Staff.StaffId" class="btn btn-sm btn-outline-info" title="View Appointments">
                                <i class="bi bi-calendar2-event"></i>
                            </a>
                            <form asp-action="Delete" asp-route-id="@item.Staff.StaffId" method="post" class="d-inline">
                                <button type="submit" class="btn btn-sm btn-outline-danger" title="Delete"
                                        onclick="return confirm('Are you sure you want to delete this staff?');">
                                    <i class="bi bi-trash"></i>
                                </button>
                            </form>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

    @if (Model.Count == 0)
    {
        <div class="text-center text-muted small mt-3">No staff found.</div>
    }
</div>
